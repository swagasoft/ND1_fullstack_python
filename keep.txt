app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql://postgres:postgres@localhost:5432/simon'
export FLASK_APP=[YOUR_APP_FILE].py
FLASK_APP=app.py FLASK_DEBUG=true flask run


from flask_demo import db, User
User.query.all()
user =  User(name='Simon Jerry')
db.session.add(user)
db.session.commit()

docker exec -ti simon-postgres psql -U postgres// log into docker postgres
flask db init // initialize migration

   // {%for d in data %}
    //     <li> <input class="check-completed" data-id="{{d.id}}"
    //          type = "checkbox" { % if d.completed %} checked {% endif %} />  {{d.description }}</li>
    //     {% endfor %}


// migrations
flask db migrate
 flask db upgrade

